<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.app.springhomework.mapper.PostMapper">
    <insert id="insert" parameterType="PostWriteRequestDTO">
        <selectKey resultType="Long" order="BEFORE" keyProperty="id">
            SELECT SEQ_POST.NEXTVAL FROM DUAL
        </selectKey>
        INSERT INTO TBL_POST
        VALUES (#{id}, #{postTitle}, #{postContent}, CURRENT_TIMESTAMP, 0, #{memberId})
    </insert>

    <update id="update" parameterType="PostUpdateRequestDTO">
        UPDATE TBL_POST
        SET POST_TITLE = #{postTitle}, POST_CONTENT = #{postContent}
        WHERE ID = #{id}
    </update>

    <select id="selectAll" resultType="PostListResponseDTO">
        SELECT TBP.ID, TBP.POST_TITLE, TBP.POST_CONTENT, TBP.POST_CREATED_AT, TBP.POST_READ_COUNT, TBM.MEMBER_NAME
        FROM TBL_POST TBP
        JOIN TBL_MEMBER TBM
        ON TBP.MEMBER_ID = TBM.ID
    </select>
</mapper>
